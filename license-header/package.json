{
  "scripts": {
    "compile": "tsc -p . && cp -R src/assets dist/assets",
    "clean": "rm -rf dist",
    "headers": "node dist/apply-headers.js '../dp-api'",
    "apply": "npm run clean && npm run compile && npm run headers"
  },
  "devDependencies": {
    "@types/node": "^16.10.9",
    "typescript": "^4.4.4"
  }
}
