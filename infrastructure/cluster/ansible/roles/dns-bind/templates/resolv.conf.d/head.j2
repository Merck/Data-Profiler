{% for host in groups['dns_primary'] %}
nameserver {{ hostvars[host]['ansible_facts']['default_ipv4']['address'] }}
{% endfor %}
{% for host in groups['dns_secondary'] %}
nameserver {{ hostvars[host]['ansible_facts']['default_ipv4']['address'] }}
{% endfor %}
search {{ bind_forward_zone }}
